import Collapse from "./El";

function CollapseComponent() {
    return (
        <Collapse>
            <p>This is the content of the collapse component.</p>
        </Collapse>
    );
}

export default CollapseComponent;
